# Vector-Store
A vector database that allows to insert and query similar vector given a vector of choice, it utilises KDTree structure and exact approximate search for querying data. There is API provided to communicate with the DB 
